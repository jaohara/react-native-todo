import ScreenView from "../ui/ScreenView";
import StyledText from "../ui/StyledText"

const OverviewScreen = ({ }) => {
  return (
    <ScreenView>
      <StyledText>I'm the Overview Screen.</StyledText>
    </ScreenView>
  );
};

export default OverviewScreen;