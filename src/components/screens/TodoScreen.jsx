import ScreenView from "../ui/ScreenView";
import StyledText from "../ui/Styled"

const TodoScreen = ({ }) => {
  return (
    <ScreenView>
      <StyledText>I'm the Todo Screen.</StyledText>
    </ScreenView>
  );
};

export default TodoScreen;