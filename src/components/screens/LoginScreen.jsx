import ScreenView from "../ui/ScreenView";
import StyledText from "../ui/StyledText"

const LoginScreen = ({ }) => {
  return (
    <ScreenView>
      <StyledText>I'm the Login Screen.</StyledText>
    </ScreenView>
  );
};

export default LoginScreen;